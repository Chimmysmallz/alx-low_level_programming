#include <stdio.h>

#include <string.h>
/**
 * prints a string, in reverse, followed by a new line
 */

void print_rev(char *s);

int main() 
{  

// use strrev() function to reverse a string 
putchar ("After the reverse of a string: %s ", strrev(str)); 
return 0;
}
